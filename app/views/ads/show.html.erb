<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @ad.title %>
</p>

<p>
  <strong>Category:</strong>
  <%= @ad.category %>
</p>

<p>
  <strong>Tag:</strong>
  <% @ad.tags.each do |tag| %>
  [<%= tag.title%>]
  <% end %>
</p>

<%= link_to 'Edit', edit_ad_path(@ad) %> |
<%= link_to 'Back', ads_path %>
