<p id="notice"><%= notice %></p>

<h1 class="text-center">Últimos Anuncios</h1>



    <div class="card">
      <% @ads.each do |ad| %>
      <div class="card-body">
        
        <h2 class="card-title">
          <% ad.tags.each do |tag| %>
          [<%= tag.title %>]
          <% end %>
        </h2>
    
        <h3 class="card-subtitle mb-2 text-muted"><%= ad.category %></h3>
    
        <h5 class="card-text"><%= ad.title %>.</h5>
    
        <%= link_to 'Show', ad %>
        <%= link_to 'Edit', edit_ad_path(ad) %>
        <%= link_to 'Destroy', ad, method: :delete, data: { confirm: 'Are you sure?' } %>
      </div>
      <% end %>
    </div> <br>



